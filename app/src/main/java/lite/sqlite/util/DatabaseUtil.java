package lite.sqlite.util;

import java.sql.Connection;
import java.sql.SQLException;
import java.sql.Statement;

import lite.sqlite.server.DatabaseManager;

/**
 * Utility class containing database helper methods.
 * This class demonstrates how to create utility methods for common database operations.
 */
public class DatabaseUtil {
    
    /**
     * Creates a backup of the database.
     * 
     * [IMPLEMENT] Create a method that backs up the database to a specified path.
     * This teaches you about database backup procedures and SQL commands.
     * 
     * @param dbManager the database manager
     * @param backupPath the path for the backup
     * @return true if successful, false otherwise
     */
    
    /**
     * Executes a raw SQL query.
     * 
     * [IMPLEMENT] Create a method that executes a raw SQL query.
     * This shows how to execute arbitrary SQL from Java and the
     * risks/benefits of doing so.
     * 
     * @param dbManager the database manager
     * @param sql the SQL query to execute
     * @return true if successful, false otherwise
     */
    
    /**
     * Checks if a table exists in the database.
     * 
     * [IMPLEMENT] Create a method that checks if a table exists.
     * This teaches you about the SQLite schema and metadata queries.
     * 
     * @param dbManager the database manager
     * @param tableName the name of the table to check
     * @return true if the table exists, false otherwise
     */
    
    /**
     * Resets a table by dropping and recreating it.
     * 
     * [IMPLEMENT] Create a method that resets a table.
     * This demonstrates data migration techniques and schema manipulation.
     * 
     * @param dbManager the database manager
     * @param tableName the name of the table to reset
     * @param createTableSQL the SQL to recreate the table
     * @return true if successful, false otherwise
     */
}
